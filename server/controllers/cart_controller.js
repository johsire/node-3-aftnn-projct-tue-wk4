const swag = require('../models/swag');
import { checkout } from '../../src/ducks/reducer';

module.exports = {
  
  add: (req, res, next) => {

  },

  delete: (req, res, next) => {

 },

  checkout: (req, res, next) => {

 }
}